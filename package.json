{
  "name": "coinview",
  "version": "1.0.0",
  "main": "dist/coinview.common.js",
  "scripts": {
    "build:common": "TARGET=common rollup -c",
    "build:window": "TARGET=window rollup -c",
    "build": "npm run build:common && npm run build:window",
    "start": "npm run serve",
    "test": "npm run lint && npm run test:coverage"
  },
  "devDependencies": {
    "@babel/core": "^7.0.0",
    "@babel/preset-env": "^7.0.0",
    "rollup": "^0.65.0",
    "rollup-plugin-babel": "^4.0.2",
    "uglify-js": "^3.4.8"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  }
}
